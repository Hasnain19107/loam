import 'package:flutter/material.dart';

class AdminMatchmakingDetailPage extends StatelessWidget {
  const AdminMatchmakingDetailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('Admin Matchmaking Detail Page - Coming Soon'),
      ),
    );
  }
}

