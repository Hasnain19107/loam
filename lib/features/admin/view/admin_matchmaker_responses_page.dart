import 'package:flutter/material.dart';

class AdminMatchmakerResponsesPage extends StatelessWidget {
  const AdminMatchmakerResponsesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('Admin Matchmaker Responses Page - Coming Soon'),
      ),
    );
  }
}

