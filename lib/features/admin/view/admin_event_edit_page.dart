import 'package:flutter/material.dart';

class AdminEventEditPage extends StatelessWidget {
  const AdminEventEditPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('Admin Event Edit Page - Coming Soon'),
      ),
    );
  }
}

